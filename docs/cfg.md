# Stage 2 —— 基本块与 CFG

> 产物 `db/blocks.json` + `src/re/BlockTable.h`。本文件由 `tools/cfgscan.py` 生成，
> 数字全部来自 `stats`，不手抄。

## 1. 做了什么

Stage 1 给出的函数体是「遍历到的最小/最大地址」围出来的**包围盒**，
函数中间是可能有洞的 —— 洞里可能是跳转表、可能是没走到的代码。
Stage 2 把每个函数拆成基本块，从而把「哪些字节真的是代码」钉死。

方法：

1. 以函数为单位做**有界**的过程内递归下降（边界就是 S1 定的 `[va, end)`，
   这里不重新找边界 —— 只相信 S1 的结论，否则两个阶段会互相漂移）。
2. leader = 函数入口 ∪ 分支目标（含跳表表项）∪ 分支的下一条
   ∪ 线性链的断点（两个可达指令之间隔着的字节是数据）。
3. 边分四类：`fallthrough` / `jcc` / `jmp` / `switch`。
   跳表里落在函数体外的表项单独记 `tail`，**不混进 switch 边** ——混在一起就没法对账了。

锚点用的是 S1 定下来的**全部 20138 个函数入口**，比 S1 自己用的
「call 目标 ∪ 虚表槽」更全。

重扫是**两步**的：先只用锚点扫一遍认跳表，再把跳表本体当**数据区**（`disasm.build_index(..., data_ranges=…)`）重扫，正式用第二遍。
少了这一步，跳表本体会被解成一串指令，「表尾那个真入口」拿不到指令边界 ——实测 `0x7C73B4` 的 `jmp` 指向表尾的 `0x7C7788`，于是留下一条「边目标在函数体内、却不是块首」的悬空边。
这一步与 S1 用的是**同一份遮罩**（都由 `JtResolver` 解出），不然两阶段的指令边界会漂移。

## 2. 规模

| 项 | 值 |
|---|---|
| 函数 | 20138 |
| 其中 ≥2 块（有 CFG 可言） | **11925** |
| 其中单块（直线代码） | 8213 |
| 基本块总数（含单块函数的 1 块） | **203071** |
| 出边总数（含单块函数的出边） | **287359** |
| └ 多块函数贡献 | 194858 块 / 287106 边 |
| └ 单块函数贡献 | 8213 块 / 253 边（其中 253 个单块函数有出边） |
| 其中 switch / 表外 tail 边 | 2706 |
| 跳表（S1 解析出的） | 331 张 |
| 重走失败（S1 走通、这里没走通的） | 0 |

## 3. 对账

| # | 判据 | 结果 |
|---|---|---|
| 1 | 块落在函数区间内、块内指令数 ≥ 1 | ✅ |
| 2 | 同函数内块按地址严格递增、不重叠 | ✅ |
| 3 | **每个块都从入口块可达** | ✅ 全部函数 |
| 4 | 遍历到的指令 100% 落进某个块 | ✅ |
| 5 | 每张跳表**逐站点**核：落成的边数 + 指向非代码的表项数 == 表项数 | ⚠️ 边数不符 0 / 未判成 switch 0 / 站点未被走到（疑似数据）14 / 站点不在函数内 2 / 含指向非代码表项 0 张 |
| 6 | 每条边要么是本函数的块首、要么是函数外（尾调用） | ✅ |
| 7 | 重走失败 | ✅ 0 个 |
| 8 | 计数下界 `b-1 <= e` | ✅ 全部函数 |
| 9 | 计数上界 `e <= 2b + c` | ✅ 全部函数 |
| 10 | 分解恒等式：总数 == 多块部分 + 单块部分 | ✅ 块数/边数都闭合 |

判据 8/9 的两条恒等式**每函数实算**，不是写在头文件注释里的装饰：
`b` = 块数、`e` = 出边数、`c` = 该函数跳表边数。下界来自「除入口块外
每块至少一条入边」（判据 3 的推论，漏一条边即破）；上界来自「块出度最多 2，
只有 switch 块能更多」。

> **与计划口径的差异（须记录）**：`docs/decompile-plan.md` §4 给 Stage 2 写的
> 对账是「块图强连通分量与 `ret` 数量一致」。这句话没法照做 —— CFG 的强连通
> 分量数是**环的个数**的量度，和函数里有几条 `ret` 之间没有可证的恒等关系
> （两条 `ret` 的直线函数 SCC 数是 2，带一个循环的单 `ret` 函数 SCC 数是 2，
> 数字碰巧相等但毫无因果）。这里改成上表 9 条**能失败、可实算**的判据：
> 计划那句真正想表达的「块图必须是良构的、且完全从入口可达」，由判据 3 + 8
> 直接测了，比原来那句更强也更有意义。跳表那条（判据 5）按原样保留。

切块过程中的可疑点计数：

| 现象 | 次数 |
|---|---|
| 条件跳转跳出函数 | 27 |
| jcc 后无 fallthrough | 1 |

## 4. 错误明细

| 现象 | 次数 |
|---|---|
| 跳表站点未被走到（疑似数据） | 14 |
| 跳表站点不在任何函数内 | 2 |

## 4b. 残差逐项定性

上表每一笔都查到了根因，**条数见上表**（这里不重复写数字，免得跟上一节漂移）。
结论：**没有一笔是 CFG 构造本身的缺陷。** 分三类。

### (1) 跳表发现器的假站点 / 假表项

`跳表站点未被走到（疑似数据）`、`跳表站点不在任何函数内`、`跳表表项指向非代码（假表项）` 是同一件事的三种表现：扫描器在**数据**里认出了「跳表站点」，或把数据的若干 dword 当成了表项。

证据（前两类全部落在两处）：

* 落在函数内的那些，全在 `0x7CA090` 与 `0x7D0A20` 两个 CRT 函数里 —— 两者 size 都是 821、12 个站点偏移逐一对应（同一份代码的两个实例）。它们那张「表」的 4 个表项指向 `0x7CA1E8` 起步处，可那里的字节是 `5E 08 45 8B …`（数据）；同函数里**真表**在 `0x7CA16C` —— 8/8 项落在 .text、7/8 是指令起点。两相对照，假站点无疑。
* 落在函数外的 2 个：形态一律是「前一个函数已结束、下一个入口还没到」的那几十字节 CRT 空隙（`0x004FF902` 在 `0x004FF700` 之后 54 字节处，`0x007C67D0` / `0x007C699A` 在 `0x007C6752` 之后那一段）。
* 假表项 0 条。这一类是**假函数**的副产品：表项落在表外、又不在任何函数的`seen` 里，说明它所依附的那个「函数」本身就是假的（表里挑几个 dword 当目标，凑不出可达代码）。为 0 表示那批假函数已经被 §4b(3) 里的修复消灭。

这三类**都不连边**（悬空边会让判据 6 里外不是人），只在 §4 记数；判据 4（遍历指令 100% 落块）为 0 恰好印证了这一点 —— **真遍历根本没走到这些站点**，所以它们从来不是待切块的指令。

### (2) `jcc` 目标落在函数区间外（记在可疑点里，共见上表）

逐条查过目标性质：目标**是函数入口**的（真·条件尾调用）占少数；
**多数既不在本函数内、也不是任何函数入口** —— 它们落在 CRT 区里S1 没能认领的字节上。

这与 S1 的函数体口径一致：S1 的 size 是**包围盒**，末端可能被下一个入口截断（`0x7C0000` 以上不受 16 字节对齐律约束，边界本就难定）。
记成 `tail` 边是把「跳到函数外」如实记下来，不是漏边 —— 判据 8（`e <= 2b+c`）与判据 3（从入口可达）都没被它破坏。

### (3) 已经在 S1 侧修掉、这里只是留痕的四处

| 现象 | 根因 | 修法 |
|---|---|---|
| `块外指令` 3 条 | Walker 半开窗口 `[lo, hi)` 的**上界闭口** bug：`if nxt > w_hi` 允许指令起点恰好落在 `w_hi`（=函数排他末端）上并被解码、进 `seen`。3 条分别是 `0x007C6789` / `0x007D0525` / `0x007D910A` | `tools/funcscan.py`：`bounds` 模式下线性推进改成 `nxt >= w_hi`，并在循环顶加了一道兜底的半开区间闸 |
| `链断在函数中间` 145 条 → 0 | **空隙通道把字节索引表当成了函数**：`0x40DD54` 起的 `01 01 01 00 01 01 …` 在 16 对齐处（`0x40DD60`）能解码成一串 `add [ecx], eax`，无控制流无 call，一路"走"到边界，于是被当成"被边界截断的函数"。每个这种假函数在 CFG 里都留一个"块末=函数末且无终止指令"的块，其中一个还造成孤儿块 | `tools/funcscan.py`：新增闸门 `only_bound_ok` —— 出口只剩不自证的 `bound` 时必须另有独立证据（calls / 栈帧 / SEH / 序言）。被拒的 98 个假函数如实退回 unknown（0.14% → 0.18%），不硬凑 |
| `跳表表项指向非代码（假表项）` 5 条 → 0 | `0x007C7757` 是紧贴跳表 `0x007C7758` 表**头前面**一个字节的 `0x90` 垫料，被空隙通道当成候选、走通、认成 size 964 的假函数 —— 它把那张 12 项跳表当成了自己的 `switch`，于是 5 个表项被当成边连出去（目标不是指令起点）。同族的还有一个 `dataptr` 假函数 `0x7C7757` 吞掉真函数 `0x7C7788` | `tools/funcscan.py`：`in_table()` 遮蔽「落在表内 **或** 距表头 1~3 字节」的候选。**方向只能是往右看** —— 往左看会把表尾那个真入口（它紧贴在表后）自己挡掉，实测正是这么误杀过一次 |
| `边指向函数内非块首` 1 条 → 0；`假表项` → 0 | **跳表本体被当成指令解码**。锚点是**点**、跳表是**区间**：`dword` 恰好能解成合法指令，扫描扎进去就回不来，于是表尾的 `0x7C7788`（函数 `0x7C7371` 内嵌跳表之后的续段）没有指令边界，而 `0x7C73B4` 的 `jmp` 正指着它 → 悬空边。同一根因还让 `0x7C7371` 从 2,942 字节被截断到 998 字节 | `tools/disasm.py` 新增 `data_ranges`（数据区遮罩：进区间直接跳到区间末重开解码）；`funcscan.py`（2a/2b）与`cfgscan.py` 都改成两步重扫，且**共用同一份遮罩** —— 两阶段用不同遮罩会让指令边界漂移 |

另外，块末恰好等于函数末端（既无终止指令、也无后继）的块已单列终止形态 `end`，不再和真故障的 `bad` 混在一起。

## 5. 最大的 20 个 CFG

| 入口 | 块数 | 边数 | 跳表边 |
|---|---|---|---|
| `0x006040B0` | 1104 | 1427 | 0 |
| `0x00618CF0` | 777 | 1238 | 36 |
| `0x005E3D10` | 545 | 872 | 0 |
| `0x00675210` | 532 | 953 | 0 |
| `0x00532150` | 527 | 829 | 0 |
| `0x004B6C30` | 458 | 736 | 0 |
| `0x00610CA0` | 455 | 738 | 0 |
| `0x0078F7F0` | 408 | 630 | 14 |
| `0x00678850` | 403 | 691 | 0 |
| `0x006BB9A0` | 382 | 533 | 0 |
| `0x006C6F50` | 370 | 554 | 0 |
| `0x00741970` | 370 | 616 | 0 |
| `0x006DD8B0` | 366 | 515 | 145 |
| `0x006691E0` | 364 | 595 | 0 |
| `0x005E00B0` | 356 | 581 | 0 |
| `0x004B2630` | 352 | 557 | 0 |
| `0x006A1C80` | 350 | 553 | 0 |
| `0x007A5890` | 345 | 556 | 0 |
| `0x0048EBF0` | 343 | 454 | 0 |
| `0x0045FE50` | 340 | 528 | 0 |

## 6. 数据结构

`db/blocks.json`：

```
"edge_kinds": ["fall", "jcc", "jmp", "switch", "tail"]
"end_kinds":  ["ret", "jmp", "jcc", "switch", "ind", "int3", "halt", "fall", "tail", "bad", "end"]
"funcs": { "0x00401000": { "b": [ [va, 指令数, 终止形态下标, [[后继va, 边型下标], ...]], ... ] } }
"single": { "0x00401xxx": "ret" }   # 单块函数：块 = [va, va+size)
"single_edges": { "0x00401xxx": [[后继va, 边型下标], ...] }
```

`single_edges` 不是可有可无的：单块函数里有一类是**纯尾调用跳板**（整段就一条 `jmp <别的函数>`），那条 `tail` 边正是 Stage 3 建调用图要用的。
早先 `single` 只存终止形态、把边丢了，于是 `stats.edges` 比 JSON 里能解包的多出 254 条、`kEdgeCount` 与文档对不上 —— 判据 10 就是钉这件事的。

`src/re/BlockTable.h`：只放**多块函数**的摘要（入口 / 块数 / 边数 / 跳表边数），
供 C++ 侧跑跨表一致性与计数恒等式。
