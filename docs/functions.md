# 函数边界重建（全量反编译 Stage 1）

> 产物由 `tools/funcscan.py` 生成，可重跑。
> 方法、阶段划分与验收判据见 `docs/decompile-plan.md`。

## 1. 为什么重做这件事

旧清单 `db/functions.json` 有 28,827 条，但其中 **22,087 条是「线性兜底」的产物** —— 它们不是函数，只是"扫描到这里，前面没有函数认领，就切一段"。
证据是入口的 16 字节对齐率：

| 来源 | 条数 | 入口 16 字节对齐率 |
|---|---:|---:|
| `entry` | 1 | — |
| `call` | 6,635 | 93.4% |
| `prologue` | 104 | 59.6% |
| `gap` | 22,087 | **4.1%** |

对齐率不是「挑出来的好看数字」，它来自一个独立测得的事实：
`0x401000–0x7C0000` 段内，**直接 call 目标与 RTTI 虚表槽目标 100% 落在 16 字节边界（12,374/12,374）**；
另有一条完全独立的佐证 —— `db/sources.json` 里由 `__FILE__` 断言字符串定位的213 个函数，VA 低 4 位全是 `0x0`（213/213）。

所以 `gap` 档 4.1% 的对齐率说明：它们 96% 落在指令中间，本就不该是函数。

## 2. 这一轮补的三条通道

### 2.1 虚表槽（最大的缺口）

`db/rtti.json` 里 949 个类、**14035 个虚表槽 / 6686 个去重目标**。
C++ 虚方法只被 `call [reg+slot]` 间接调用，**永远不会**被 `call rel32` 播种找到。
实测：其中只有 **455 个（6.8%）** 曾被旧清单的强证据通道找到。

### 2.2 跳表

`jmp dword ptr [reg*4 + 表址]` 实测 387 处（绝对形式），解析出 **369 张跳表 / 2706 个表项**；
另有 0 处「表址在寄存器里」的形式（`lea reg,[tab]; jmp [reg+reg*4]`），本轮不解析，只计数。
旧实现只跟 `X86_OP_IMM` 操作数，**完全忽略跳表** → 带 `switch` 的函数被子截断。

### 2.3 尾调用

`jmp` 落到跟随窗口之外 → 判为尾调用，既是本函数的出口，也是**目标函数的入口证据**。旧实现丢弃了这个信息。

### 2.4 索引锚点重同步

线性扫描只在**解码失败**时重新对齐。`.text` 里嵌着数据，数据往往能被解成一串合法指令，此时扫描会一直跑偏、再也不回来。
`tools/disasm.py` 现在支持 `anchors`：扫描一旦跨过已知入口就丢弃这条指令、改从锚点重新开始。锚点由第一遍扫描的结果算出，所以是两遍扫描。

## 3. 结果

| 项 | 数量 |
|---|---:|
| 函数 | **19533** |
| └ 主档 `entry` | 1 |
| └ 主档 `vtable` | 6686 |
| └ 主档 `call` | 6278 |
| └ 主档 `tail` | 5 |
| └ 主档 `padend` | 6212 |
| └ 主档 `prologue` | 2 |
| └ 主档 `dataptr` | 349 |
| 指令（第二遍扫描） | 1392509 |
| `.text` 字节 | 4060045 |
| └ 代码 | 3788493（93.3%） |
| └ 填充（连续 ≥4 的 `0x90`/`0xCC`） | 147680（3.6%） |
| └ 数据（跳表本体） | 9656（0.2%） |
| └ **未知** | **114216（2.81%）** |

## 4. 对账

每一条都是**可以失败的**，不是自证。

| # | 判据 | 结果 |
|---|---|---|
| 1 | `0x007C0000` 以下函数入口 16 字节对齐率必须 100% | 18945/18945 = 100.00% ✅ |
| 2 | 以上（运行时库区）不受约束，但要对上账 | 588 个，对齐 109（18.5%） |
| 3 | `db/sources.json` 的 213 个函数必须全部在结果里 | ✅ 全部命中 |
| 4 | 函数区间两两不重叠 | 原始重叠 114263 字节；按证据强度去重后 0 字节，丢弃 193 个弱证据入口 ✅ |
| 5 | 每个函数终止于 `ret` / 尾跳 / thunk | ✅ 全部有出口 |
| 6 | PE 入口点必须在结果里 | ✅ |
| 7 | 旧清单 `call` 档 6,635 条必须全部保留 | ⚠️ 未保留 4 个（见 §6） |
| 8 | 未知字节 < `.text` 的 1%（本阶段目标） | ❌ 实为 2.81% —— **没达成**，残差见 §5 |
| 9 | `.text` 每字节恰好属于 代码/填充/数据/未知 之一 | ✅ 4060045 = 4060045 |

旧清单的 22,087 条 `gap` 里，有 599 条恰好落在新结果认下的函数区间内 ——
**这不代表它们被"确认"了**：它们本来就和真函数重叠，地址偶然落在里面而已。

## 5. 残差：未知字节 —— 本阶段目标没达成

`unknown` = 既不属于任何函数、又不是连续填充、也不是跳表本体。
共 **3812 段 / 114216 字节 = `.text` 的 2.81%**。§4 第 8 条要求 < 1%，**没做到**。

| 段大小 | 段数 | 字节 |
|---|---:|---:|
| >=8192 | 1 | 14360 |
| 2048-8191 | 5 | 12357 |
| 512-2047 | 36 | 35298 |
| 128-511 | 116 | 27193 |
| <128 | 3654 | 25008 |
| **合计** | **3812** | **114216** |

最大的 15 段：

| 起 | 止 | 字节 |
|---|---|---:|
| `0x00618CE4` | `0x0061C4FC` | 14360 |
| `0x005FB31F` | `0x005FBEE9` | 3018 |
| `0x005DE51F` | `0x005DEFD0` | 2737 |
| `0x00596300` | `0x00596C54` | 2388 |
| `0x007C4730` | `0x007C4F8A` | 2138 |
| `0x0061418F` | `0x006149AB` | 2076 |
| `0x007C6B70` | `0x007C7350` | 2016 |
| `0x00560480` | `0x00560BE9` | 1897 |
| `0x005E691F` | `0x005E7044` | 1829 |
| `0x007C8362` | `0x007C8962` | 1536 |
| `0x00616384` | `0x00616914` | 1424 |
| `0x0060A5AF` | `0x0060AA92` | 1251 |
| `0x007C4260` | `0x007C4725` | 1221 |
| `0x007D7FF7` | `0x007D8496` | 1183 |
| `0x0079DB50` | `0x0079DFDA` | 1162 |

这些字节**不做外推**：不说它们是代码，也不说它们是数据。

### 5.1 本轮查清并修掉的三个成因

每一个都是先实测复现、再改代码的：

1. **锚点偏移算错**（`tools/disasm.py`）。锚点传进来的是 VA，内部要的是相对 `.text`
   的偏移，代码写成了 `VA - lo`（漏了 ImageBase），所有锚点整体偏了 `0x400000`，
   重同步机制**静默失效**。修掉之后，一批强证据入口从「不是指令起点」变为可遍历。
2. **包围盒吞函数**。函数的字节区间取的是 `[min(seen), max(seen)+len]`，是个包围盒；
   而遍历完全可能整段跳过中间（条件跳转直接跳到后面）。
   实测 `0x407040` 因此吞掉 `0x407070` 起一整簇小函数，跨度 14.7 KB。
   修法：强证据入口落在包围盒内就把末端截到它 —— 本轮截断 **6** 个函数。
3. **跳表不跟**。387 处 `jmp dword ptr [reg*4+表址]`，解析出 369 张表 / 2,706 个表项。
   旧实现只跟立即数操作数，带 `switch` 的函数会被截断。

三轮的效果：

| 轮次 | 未知字节 | 当轮做的事 |
|---|---:|---|
| 1 | 522,751（12.88%） | 只有 entry / call / 虚表三条通道 |
| 2 | 142,366（3.51%） | 加「填充尽头」通道 + 遍历路标 |
| 3 | 114216（2.81%） | 修锚点偏移 + 截包围盒 |

### 5.2 还没修的成因（下一轮）

- **大函数体内嵌查表数据**。`0x618CE4` 那段 14,360 字节是一例：函数开头是
  `sub esp, 0x240` 这类巨大栈帧，体内夹着 CRC 之类的查找表；遍历走进表里会解成
  一串假指令，最后被密度校验毙掉。要等 S2 能区分「代码块 / 数据块」之后才好处理。
- **只被间接调用、且不出现在任何表里的函数**。没有任何一条通道能看到它们。
- **表址在寄存器里的跳表**（`lea reg,[tab]; jmp [reg+reg*4]`）。本轮统计为 0 处 ——
  这个数字本身要留着：它说明这条形式**没出现**，而不是我们解析过了。

## 6. 被丢弃与遍历失败的入口（一条都不藏）

### 6.1 遍历失败 595 个

| 原因 | 个数 |
|---|---:|
| 不是指令起点 | 501 |
| 向前回溯（入口选错） | 76 |
| 密度校验不过（跨度里混进数据） | 3 |
| 没有出口 | 15 |

按档分：`dataptr 588`, `call 8`, `padend 1`。绝大多数是 `dataptr` 档 —— 函数指针表里混着指向函数**内部**的地址，
这些地址本来就不是入口，被拒是对的。

### 6.2 去重叠丢弃 193 个

去重叠的规则是「证据强的先占」。丢弃的全部落在更弱的档上：`dataptr 191`, `padend 2`。

| VA | 档 | 被覆盖字节 |
|---|---|---:|
| `0x00407060` | padend | 10 |
| `0x004E60C0` | padend | 114 |
| `0x0040F7B0` | dataptr | 11 |
| `0x0040F8C0` | dataptr | 41 |
| `0x00410700` | dataptr | 17 |
| `0x004107A0` | dataptr | 7 |
| `0x00410840` | dataptr | 276 |
| `0x00410B40` | dataptr | 61 |
| `0x00413C50` | dataptr | 7 |
| `0x00416570` | dataptr | 77 |
| `0x004166B0` | dataptr | 3 |
| `0x0041B8B0` | dataptr | 71 |
| `0x0041CB30` | dataptr | 27 |
| `0x00425450` | dataptr | 188 |
| `0x0042D550` | dataptr | 26 |
| `0x00436D80` | dataptr | 17 |
| `0x00447AA0` | dataptr | 22 |
| `0x0044EFC0` | dataptr | 1525 |
| `0x00453DB0` | dataptr | 5 |
| `0x00462430` | dataptr | 9766 |
| `0x00463B70` | dataptr | 3814 |
| `0x00497110` | dataptr | 216 |
| `0x004A88B0` | dataptr | 5 |
| `0x004ABDC0` | dataptr | 1258 |
| `0x004B9900` | dataptr | 716 |

### 6.3 旧清单 `call` 档没有保留下来的 4 个

| VA |
|---|
| `0x004F4D80` |
| `0x005175D0` |
| `0x006BEC50` |
| `0x007DCD9C` |

旧清单那 22,087 条 `gap` 里，有 599 条恰好落在新结果认下的函数区间内。
**这不能算「被确认」**：它们本来就和真函数重叠，地址偶然落在里面而已 ——
4.1%% 的入口对齐率已经说明它们不是函数。

## 7. 出口形态

| 出口形式 | 处数 |
|---|---:|
| `ret_imm` | 16002 |
| `ret` | 14177 |
| `tail` | 367 |
| `switch` | 315 |
| `thunk` | 116 |

`ret N` 给出的 `__stdcall` 参数字节（下一步做签名时要用的现成通道）：

| `ret N` | 函数数 |
|---|---:|
| `4` | 4908 |
| `8` | 1997 |
| `12` | 723 |
| `16` | 523 |
| `20` | 129 |
| `24` | 68 |
| `28` | 120 |
| `32` | 138 |
| `36` | 79 |
| `40` | 69 |
| `44` | 58 |
| `48` | 1 |
| `52` | 1 |
| `56` | 2 |
| `60` | 1 |
| `64` | 3 |
| `68` | 1 |
| `112` | 3 |
| `232` | 1 |

## 8. 已知局限

- **三分之一入口只靠启发式撑着**。19,533 个函数里，主档为 `padend` 的有 6,212 个 ——
  它们靠的是「16 字节对齐 + 前面是填充」这条规律，而不是直接调用或虚表。
  这条规律本身是实测的（本体区 12,374/12,374），但落实到单个函数上，
  它的强度明显弱于 `vtable` / `call`。JSON 里每个条目都带 `tier`，用的时候要看得见。
- **0 个函数的边界可疑**（密度校验不过，但因为入口证据强而保留，记 `low_density`）。
- **6 个函数的末端被截断**（包围盒里撞上别的入口）。截断是对的，但它说明这些函数的内部结构还没被读懂。
- **`0x7C0000` 以上不受对齐律约束**，那段的边界质量明显低于本体区，本工具只做「有证据就采」，不假装和本体区一样可靠。
- **不做外推**：`unknown` 段不猜、不填。宁可留着，也不制造假边界。
- 重叠、无出口这类异常一律列出来，不静默丢弃。
